# Feed-Model Lab Archive

This archive preserves the historical feed-model implementation and its
diagnostic tests without presenting it as part of the default runtime.

The files under `src/` and `tests/` remain intentionally close to their
original layout so focused experiments can still be built behind the
`experimental_feed_model` Meson option.
